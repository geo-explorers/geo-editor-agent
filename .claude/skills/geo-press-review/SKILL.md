---
name: geo-press-review
description: Compare external press coverage (Google News / web) against what's published on Geo, and tell editors what to publish next. Classifies stories as already-published or not-yet-covered, ranked and justified. Also does source discovery for a topic+date. Read-only — it compares and recommends, never publishes. Triggers on "press review", "what's missing on Geo", "compare press coverage", "what should we publish", "is this covered", "find sources for", "coverage gaps", "timeline for", "what news did we miss".
metadata:
  version: "0.5.3"
  stub-for: skills/non-actionable/geo-press-review/SKILL.md
  tier: non-actionable
---

# geo-press-review — routed skill

This is a discovery stub. The skill itself lives in the toolkit at
`skills/non-actionable/geo-press-review/SKILL.md` (upstream layout, kept intact so it can be synced from content-management).

**Read that file now and follow it exactly.** It is the contract — it carries hard rules,
gates and the scripts' real paths, none of which are repeated here.

Run its scripts from the **repository root** (`.env`, `src/` and `skills/` resolve from there).

This skill is **read-only on Geo**.

Regenerated by `tools/gen-skill-stubs.mjs` — do not edit by hand.
