---
name: image-banner-recompose
description: Intelligently recomposes any uploaded image into a 2364 × 640 px banner. Use this skill
metadata:
  version: "0.2.0"
  stub-for: skills/non-actionable/image-banner-recompose/SKILL.md
  tier: non-actionable
---

# image-banner-recompose — routed skill

This is a discovery stub. The skill itself lives in the toolkit at
`skills/non-actionable/image-banner-recompose/SKILL.md` (upstream layout, kept intact so it can be synced from content-management).

**Read that file now and follow it exactly.** It is the contract — it carries hard rules,
gates and the scripts' real paths, none of which are repeated here.

Run its scripts from the **repository root** (`.env`, `src/` and `skills/` resolve from there).

This skill is **read-only on Geo**.

Regenerated by `tools/gen-skill-stubs.mjs` — do not edit by hand.
