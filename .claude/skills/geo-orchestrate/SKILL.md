---
name: geo-orchestrate
description: Editor-facing entry point for Geo workflows. Translates natural-language intent ("find X and merge into Y", "assign Topics to all Claims about Bitcoin", "publish a podcast episode", "deduplicate Persons", "add Web URL to every Project") into a query plan + a publish plan, generates a runnable script, dry-runs it, confirms with the editor, then publishes. Triggers on "I want to", "find and merge", "deduplicate", "assign", "bulk update", "for all entities of type", "edit Geo", "fix", "clean up", "merge duplicates".
metadata:
  version: "0.2.0"
  stub-for: skills/actionable/geo-orchestrate/SKILL.md
  tier: actionable
---

# geo-orchestrate — routed skill

This is a discovery stub. The skill itself lives in the toolkit at
`skills/actionable/geo-orchestrate/SKILL.md` (upstream layout, kept intact so it can be synced from content-management).

**Read that file now and follow it exactly.** It is the contract — it carries hard rules,
gates and the scripts' real paths, none of which are repeated here.

Run its scripts from the **repository root** (`.env`, `src/` and `skills/` resolve from there).

This skill can **write to Geo**. Every write goes through its own dry-run → explicit confirmation gate. Never bypass it.

Regenerated by `tools/gen-skill-stubs.mjs` — do not edit by hand.
