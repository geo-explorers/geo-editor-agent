---
name: geo-mirror
description: Mirror ANY Geo entity type from ANY space into Notion as linked databases, and (Part 2) sync reviewed Notion edits back to Geo. Type-generic — News stories, podcast Episodes, Events, People, etc. — one Notion database per entity type (primary + each related type), keyed by Geo ID so re-runs update in place. Read-only on Geo in Part 1. Triggers on "mirror to notion", "geo to notion", "export space to notion", "sync geo into notion", "mirror podcast into notion", "mirror episodes/events into notion".
metadata:
  version: "0.13.0"
  stub-for: skills/actionable/geo-mirror/SKILL.md
  tier: actionable
---

# geo-mirror — routed skill

This is a discovery stub. The skill itself lives in the toolkit at
`skills/actionable/geo-mirror/SKILL.md` (upstream layout, kept intact so it can be synced from content-management).

**Read that file now and follow it exactly.** It is the contract — it carries hard rules,
gates and the scripts' real paths, none of which are repeated here.

Run its scripts from the **repository root** (`.env`, `src/` and `skills/` resolve from there).

This skill can **write to Geo**. Every write goes through its own dry-run → explicit confirmation gate. Never bypass it.

Regenerated by `tools/gen-skill-stubs.mjs` — do not edit by hand.
