# Skill versions & changelog

Per-skill version history. Pairs with `SKILL-VERSIONS.json` (machine-checkable integrity: approved commit + content hash) — this file is the **human "what changed and why"**.

**On every approved change to a skill:**
1. Bump the `version` in that skill's `SKILL.md` frontmatter (semver: patch = wording/fix, minor = new capability, major = breaking workflow change).
2. Add a dated bullet under that skill below.
3. Regenerate the manifest: `python3 skill-dev/skill_versions.py generate`.
4. Commit all three together.

---

## non-actionable/

### geo-mirror-and-group — 1.0.0
- 2026-09-18 · **New skill — one runner for the whole mirror → group pipeline.** The pipeline was eight scripts across three skills; run by hand that is eight round trips, a campaign directory invented on the spot, and two settings that are silently wrong more often than right. `scripts/run-pipeline.mjs` chains the mechanical stages (`probe` → `mirror` → `prepare` → `finish`), keeps adjudication between `prepare` and `finish` where judgement belongs, and records its decisions in `pipeline.json`. It derives the two settings instead of defaulting them: `probe` checks whether the space holds News stories and turns semantic recall on for debate-shaped corpora (with no stories the shared-story-AND-shared-topic branch is unsatisfiable for every pair, leaving text overlap — the one gate a same-meaning-different-wording pair fails by construction), and `prepare` sizes `--top` to the both-in-roster count so an adjudication is never a sample that reads as a pass. `probe` uses `entitiesConnection.totalCount` instead of paging the type: an AI-space probe drops from over two minutes to under four seconds. Read-only on Geo; never runs geo-mirror Part 2; never passes the sink's `--publish`. Verified end to end on 30 AI claims: probe → mirror → prepare → 26 pairs adjudicated → dry-run → publish, 27/30 rows, read-back 0 differences.

### notion-operations — 1.0.1
- 2026-09-18 · **Stop naming one editor's local subagent.** The two-identities section named a subagent that exists only in one contributor's private workspace, as though every reader had it. Everything in this repo is used by other people; the guidance now says to check a subagent's own tool list rather than assuming which agent is running.

### notion-operations — 1.0.0
- 2026-09-18 · **New skill — the shared substrate under geo-mirror and geo-claim-grouping-notion.** The toolkit reaches Notion two ways (integration token from scripts, MCP connector as the editor) and the difference is invisible until it bites: a page the editor can open still 404s for a script until it is connected, and a connector edit can leave no trace the integration can attribute. Documents which identity to use, what to check before writing, how to pace and verify a bulk write, and the traps that return a plausible partial answer instead of an error. Also records defects no script reports — the legacy `Notion-Version: 2022-06-28` pin across all six scripts, which works only while every mirrored database has a single data source.

### geo-claim-grouping-notion — 0.2.0
- 2026-09-18 · **Two silent partial results made loud.** `roster-from-notion.mjs` is schema-agnostic by design, which made a wrong-type mirror silently valid — it read a Topics database, accepted all 70 `Topic` ids as a claims roster, and printed a clean success summary; only an external check caught it. It now re-resolves every kept id against Geo (read-only GraphQL, no wallet, no env) and exits 2 unless all carry the expected type, recording the outcome in `roster.json` under `typeCheck` (`--type` for another type, `--skip-type-check` offline). `write-grouping-to-notion.mjs` now reads `capped` and `scoped.keptBeforeCap` from `candidates.scoped.json` and reports a capped scope in **Needs your eyes** — an adjudication of 80 of 135 pairs previously printed `nothing` there and read as a complete pass.

### geo-query — 0.2.9
- 2026-09-08 · **Scope multi-space entity reads by `spaceId`** (editor: querying an entity's properties returns copies from *all* spaces it lives in, merged — inflated/confusing; should return the named space's view, or Root if none given). `entity.values` and `entity.relations` aggregate every space the entity lives in — an unscoped read of a multi-space entity merges each space's copy. Both accept a `spaceId` filter: `values(filter:{spaceId:{is:"…"}})` / `relations(filter:{spaceId:{is:"…"}})`. New rule (extends "Multi-space entities"): scope reads to the space the user named (canonical id), or default to **Root** (`a19c345a…`) when unspecified; only read unscoped for a deliberate cross-space view. Verified live: "Foundation Models" (2 spaces) unscoped = 2 values / 8 relations, scoped = 1 value / 5 relations. Gotcha 17.

### geo-query — 0.2.8
- 2026-08-28 · **Hardcoded canonical space name→ID map — the space-scoping fix** (Arturas' list; recurring bug where live space-name resolution fuzzy-matches to the *nearest* space — a real case matched "World affairs" → "AI" — silently scoping a whole query to the wrong space and returning confident-but-wrong results). Added a "Canonical spaces (name → ID)" table to Well-known IDs: 13 canonical + 5 dataset spaces, all IDs **verified live 2026-08-28** against their `space.page.name`. Rule: scope canonical/dataset spaces with the hardcoded ID, never `space(name:)`/`search()`; for a space NOT in the table, resolve live but confirm `id`+`page.name` match exactly and STOP on substitution. Gotcha 16. Note on naming: ID `4582fbbe…` is live-named **"US Politics"** (not "U.S. Politics") and its dataset **"US politics datasets"** — table keys the live names + notes the "U.S. Politics" alias. Mirrors `src/constants.ts` CANONICAL_SPACE_IDS/DATASET_SPACE_IDS for the actionable skills (geo-query is browser-capable so it can't import them — the table is its standalone copy).

### geo-query — 0.2.7
- 2026-08-24 · **Memory blow-up anti-pattern — don't teach the OOM query shape** (geo core team / Patrick: a `postgres_to_geo` sweep opened `entitiesConnection(first:1000)` with nested `relations(first:1000)` per node — multiplicative hydration that OOM-killed the testnet API pods, ~28 requests → 5 restarts; also >30 s so it timed out client-side and never succeeded). The skill's "max page size everywhere: `first: 1000`" line read as a default. Reframed it as a hard **ceiling, not a default**, and added a Performance "Memory blow-up" callout: when nesting per-node relations keep the **root page ≤ 100** AND **filter nested relations by `typeId`** (~1 hydrated per node, not ~1000, and it reaches >1000-relation topics). Clarified the flat `relationsConnection` bulk scan stays safe (flat, not `nodes × relations`). Gotcha 15. Our own generators were already safe (press-review coverage map uses root `first:100`; `lib/gql.mjs` nests `relations(first:100)` on single entities) — this hardens the *guidance* so agents don't write the bad shape.

### geo-query — 0.2.6
- 2026-08-12 · **"Published" timestamp disambiguation** (field report: "how many news stories published in the last 9 hours" answered **0**, true answer **13** — the agent filtered on the wrong field). A News story has two times: the **`Publish datetime`** property (`94e43fe8…`) = source-outlet dateline, and entity **`createdAt`** = when it was added to Geo. Editors' "published recently" means added-to-Geo → filter `createdAt`, not the property (the ingestion pipeline bulk-adds stories hours after their dateline). Added a "'Published' is two different timestamps" section (table + rule + per-space `createdAt` count query + migration caveat that pre-migration `createdAt` is flattened), the `Publish datetime` property ID to Well-known IDs, and gotcha 14.

### geo-query — 0.2.5
- 2026-07-27 · **v20 migration:** GraphQL endpoint → `api-testnet.geobrowser.io/graphql` (old `testnet-api.…` keeps working ~a week post-migration, then dies; note the word-swap — easy to misread). `lib/gql.mjs` re-pointed and verified live against the new endpoint. No query-shape changes — endpoint URL only.

### geo-query — 0.2.4
- 2026-07-17 · **Multi-space types are not duplicates** (field report: the skill's output sent editors dedup-hunting on healthy entities): new "Multi-space entities" recipe — `entity.types` repeats one Types edge per space; verdict = group edges by relation `spaceId`, cross-space repeats are healthy assertions, only same-space repeats are true duplicates (named by space; cross-space deletes are silent no-ops). Verified live on Bitcoin `2f8238b2…`: 6 Topic edges = 3 healthy spaces + one space with a genuine triple (the naive reading is wrong in both directions). Gotcha 13 + eval 8.
- 2026-07 · **Canonical client + trap table** (kills the hand-rolled-curl error class: 59 schema errors / 31 re-introspections / 236 ad-hoc curls across a month of sessions): new `lib/gql.mjs` (zero-dep `query()` with retry+backoff that throws the FULL GraphQL error text — never swallows into empty `data`; verified templates `Q.ENTITY_BY_ID` / `Q.ENTITIES_BY_TYPE(_CONNECTION)` / `Q.BACKLINKS` / `Q.PROPOSALS`; `paginate()` cursor helper) + `lib/gql-cli.mjs` shell one-liner. New SKILL sections "Canonical client" + "Fields that DON'T exist (recurring mistakes)" — no `nodes` on flat lists, Entity has `spaceIds` not `spaceId`, RelationFilter takes `typeId` not `type`, backlinks scoped via `filter` not a spaceId arg, filter fields take filter OBJECTS, no `equalTo` anywhere; every row verified against live introspection. curl section demoted to fallback; all 4 templates verified live (crypto space). Rebased onto geo-publish 0.5.1; hand-rolled TS cursor loop + curl block condensed to canonical-client pointers (keeps body <500 lines).
- 2026-07 · **Row order inside tables + front-page/tabs recipe** (the missing half of the shuffled-tables report — 0.2.1 ordered the *blocks*, rows within each table still followed `createdAt`): query-block rows follow the block's `Sort` **value** (`sort_by` property + direction — verified live: timeline block = Publish date desc); collection-block rows = `Collection item` relations by `position` (null-tolerant). Review-a-submission steps 3–4 updated (decode block KIND + keep its own order). New "Front pages and tabs" recipe: `space.page` front-page discovery + tabs by `POSITION_ASC` (verified: AI front page's 10 tabs match rendered order). Gotcha 11 extended.
- 2026-07 · Merged colleague's 0.2.1 refinements (all claims re-verified live): dedicated "Relation properties — read them inline" core section; pagination table corrected (`first`/`offset` ≤ 1000 on both shapes); Pattern 3 = filterable `backlinks`; Blocks relation-type ID `beaba5cb…` in the review query + Well-known IDs; `createdAt` keyset note; `backlinksExist`; `searchConnection`/`similarityThreshold`; `typeEntity{}` note.
- 2026-07 · **Performance + freshness fixes** (editor feedback: "topic query takes 5–10 min"): new Performance section — never N+1, use server-side relation filters (measured live: Iran-war stories 469ms vs ~131s+ N+1); bulk-scan pattern (relationsConnection 1000 rows/330ms with inlined nested fields). New Data-freshness section (verified: no cache, indexer lag ~seconds; re-query, don't distrust). Review-a-submission: block/table order = relation `position` (server `orderBy: POSITION_ASC`; verified vs live UI). Gotchas 9–12.
- 2026-07 · Hardened by independent 43-check live test: fixed StringFilter exact-match (`is`/`isInsensitive`, not `equalTo`); documented `first`≤1000 + offset-cap-on-Connections (cursor-only past 1000); `entity(id:)` never-null stub + correct existence check; one-query inline relation-properties + `backlinks` + count-only/`search()`/`relation(id:)`/`property(id:)`/`relationsExist` extras; softened stale unscoped-500 claim to performance rule; evals: dead fixture replaced, 4 new evals covering the v0.2.0 patterns.
- 2026-06 · Initial. GraphQL-over-HTTP querying (Nick's version); runs anywhere incl. browser. Folded in submission review/fact-check (merged from the retired geo-query-web). Person-ID corrected to `7ed45f2b…`.

### ontology-advisor — 0.1.0
- 2026-06 · Initial. Conversational modelling advice grounded in ONTOLOGY.md + live graph; read-only.

### geo-press-review — 0.5.3
- 2026-07-27 · **v20 migration:** GraphQL endpoint → `api-testnet.geobrowser.io/graphql` in SKILL.md + `scripts/press-review-coverage-map.ts`; coverage map re-verified live against the new endpoint. Query shapes unchanged.

### geo-press-review — 0.5.2
- 2026-07-27 · **Eval-driven fixes (read-only eval of 0.5.1, all findings verified live).** Coverage-map script: replaced the per-story N+1 (`fetchStory` × ~1,395) with a single paged `entitiesConnection` pulling values+relations inline — **193s → ~10s** (~19× faster, benchmarked on World affairs); added `world affairs` + `us politics` to the `SPACES` map (the flagship example previously hard-errored) and now logs any fuzzy space resolution; added a `⚠ NO/THIN BASELINE` warning when a space has ~0 in-window / <10 total stories (empty spaces used to print three reassuring "(none)"s). SKILL.md: re-check query `first: 5 → 25` + truncation guard (the `first:5` truncation once hid a real ICC-withdrawal match, nearly recommending a duplicate); new cross-space re-check step for off-beat 🆕s (an Ebola story lived in health, invisible to a World-affairs review); conflicting-numbers rule in the source-faithfulness gate (cite the range, never a single unbacked figure); publish-date-typo noted as a *cause* of window misses (a real story dated 2025 vs its 2026 text); note that the batched rigor checks cost only ~6s so they must not be skipped. Eval #1 corrected from the removed three-bucket model to the current two-bucket output.
- 2026-07 · Version bump to 0.5.1 (integrity manifest) + claims-read query widened to `relations(first: 100)` so busy stories don't drop claims. Consolidates the connector guard + mandatory-claims + two-bucket + accuracy work below into one shippable version.
- 2026-07 · Connector-safety guard (Ahmed's report): Half 1 must use the **GraphQL API by space ID**, never the Hypergraph MCP connector — the connector exposes only a curated space subset (no World affairs) and **silently fuzzy-matches a space name to the nearest one** ("World affairs" → "AI"), which would build a coverage map of the wrong space. Added a mandatory space-verification guard (confirm resolved `id`+`page.name` exactly; STOP on not-found/mismatch/substitution). Confirmed live: `89bd89bf…` = World affairs, 1,175 News stories, reachable via GraphQL.
- 2026-07 · Retest fix: the claims-check is now **MANDATORY** for any "Geo has a related story but not this thread" 🆕 (was judgment-based; one run skipped it and mis-flagged the "Mojtaba absent from funeral" thread as new). Editor called it launch-confident on Claude after this.
- 2026-07 · Accuracy round from an editor live-test (World affairs Jul 3–6): before 🆕, re-check with an **undated search of the News-story TYPE** (catches stories Geo published just outside the window — the India–Japan miss); **match on the story's `Notable claims`, not just title+description** (the Kyiv "68 missiles/351 drones" facts were already claims). Both queries verified live. Also hardened the two-bucket scope note (removed the 🔄 label from the instruction so it stops re-planting the forbidden bucket).
- 2026-07 · **Removed the 🔄 "On Geo — needs update" bucket** (editor call: not useful in current form, deferred to a future version) → two-bucket output (✅ Already on Geo / 🆕 Not on Geo yet). **Headlines are now clickable links to their primary source** (editor's top-requested shortcut — click the headline, land on the article). Cross-Geo `entity` check + ✅ entity-ID citation retained.
- 2026-07 · A/B tuning (Claude-vs-GPT test on World affairs Jul 3–6; gap was process depth, not search quality): mandatory 8–10 topic-lane fan-out before searching; discard out-of-window article dates; non-US compensation (outlet-name queries).
- 2026-06 · Source-faithfulness gate: a cited source must actually substantiate the *specific* story (not just the broad topic), and the recommendation may only be as specific as its sources support — fixes attaching generic "Iran war — live" blogs to a precise story. Prefer specific articles over live-blogs/topic hubs.
- 2026-06 · Editor-friendly labels ("Not on Geo yet" instead of "not covered"); all sources rendered as clickable links.
- 2026-06 · Initial. External press (Google News/web) vs Geo coverage; three-bucket ranked output.

### qa-report-workflow — 0.1.0
- 2026-06 · Initial. Triage + report UI/UX bugs (editor-reported, or active hunting via Playwright MCP); copy-paste Linear/Notion/Slack output; markdown report file. *(Not yet committed/shipped.)*

### image-banner-recompose — 0.2.0
- 2026-06 · Initial in-repo. Recomposes an uploaded image (e.g. a book cover) into a 2364×640 banner — smart crop / outpaint / blurred backdrop / solid-edge extend, auto-picked per image; strategy-aware QA, unsharp finishing, low-res warning. Author: Armando; reviewed + rebuilt into a full skill by Moh. No graph writes.

### geo-describe — 0.2.0
- 2026-06 · Fleshed out the accuracy/research leg: added `references/accuracy-verification.md` (Tier 0/1/2 verification, verify-framing-not-just-existence, dynamic-metric handling, verification-record format, faithfulness check) and wired it into SKILL.md Stages 3 & 5. Validated on a real run (Aave): verified founder/origin, confirmed a TVL ranking (softened as a dynamic metric), corrected a distorted claim ($1T cumulative mis-stated as daily).
- 2026-06 · Initial. Writes accurate, original, rules-compliant entity descriptions at scale: fact-extract → tiered parallel accuracy research → compose source-hidden → originality+accuracy+rules gate (`scripts/check_similarity.py`) → emit vetted description + Source for geo-publish. Makes no graph writes itself (hands off to geo-publish); advanced/needs the fuller Claude env (see `compatibility`). Author: Armando (spec), Moh (build).

### daily-report — 0.1.0
- 2026-06 · Initial. Editor end-of-day daily update: reads the day's Claude Code sessions (`scripts/gather_sessions.py`, runtime read, token-light), distills the Geo work into bullets, asks a top-up for work done elsewhere (Cowork/chat/ChatGPT), confirms, then files the editor's row to the Notion daily-update form under their own identity. Manual; no graph writes. v1 of the editor-scalable routine (scheduling deferred to v2).

### geo-claim-grouping-notion — 0.1.0
- 2026-09-14 · **Initial.** Notion sink for geo-claim-grouping: run the parent skill's Stage A discovery (unmodified, scoped to the mirror via `--scope-file`) and Stage B adjudication, then write the results into the Notion Claims mirror as review columns instead of Geo proposals — `Proposed exact duplicates`, `Proposed semantic duplicates`, `Proposed related claims`, `Proposed supporting / opposing arguments` (self-relations, one-way, Geo direction semantics copied from the ops template) + `Proposed grouping notes` (one hyperlinked line per counterpart with tag · confidence · reason). Four zero-dep Node scripts: `roster-from-notion.mjs` (Claims DB → roster = `--scope-file` shape), `scope-candidates.mjs` (keep only pairs with BOTH sides in the mirror — discovery keeps either-side matches), `existing-edges-from-geo.mjs` (Root Related/Duplicate/Similar/Supporting/Opposing edges between roster claims — discovery's `alreadyRelatedClaims` is the legacy Crypto property), `write-grouping-to-notion.mjs` (schema-agnostic sink: needs only `Geo ID` + title; creates its six columns, never re-types or touches any other column; skips edges already on Geo per direction; diff-first; additive by default, `--prune` opt-in; dry-run default with a mandatory six-section report incl. "Needs your eyes"; `--publish` then read-back must report 0 remaining). Non-actionable: never writes Geo. Verified live read-only on the Relationships test mirror (186 rows) and on a fixture campaign covering every bracket/direction/skip class.

## actionable/

### geo-claim-grouping — 0.7.0
- 2026-09-18 · **Semantic recall no longer reaches outside the space being worked.** S3c ran a `search()` that takes no space argument, so semantic recall admitted hits from anywhere in the graph — silently reintroducing the cross-space matching `--pool` exists to opt into, even though S1/S2 already honoured that bound. A scoped run could pull claims from spaces it never named. Hits must now be resident in `--space` or an explicitly pooled space, making `--pool` the single control for cross-space behaviour across the whole pipeline. `summary.json` gains `semanticScopeSpaces` and `semanticOutOfScopeDrops`, and the S3c log line reports the drops, so a run records its own scope rather than leaving it to be inferred. Verified live on the AI space: 235 out-of-scope hits dropped against a single scope space.

### geo-mirror — 0.12.0
- 2026-09-18 · **`--ids-file`, and `--link ""` fixed.** (Authored against 0.9.0 before 0.11.0 landed; released as 0.12.0 on top of it.) Every existing scope flag (`--since`, `--related`, `--limit`) is applied *after* the entire type has been paged, so on a 22,000-claim space each still pays for the full read — and `--limit N` returns an arbitrary N when the type carries no date property. `--ids-file <path>` skips the sweep and fetches exactly the entities named, in batches, satisfying the no-unscoped-mirror gate on its own; ids that do not resolve, are not resident in `--space`, or are not `--type` are reported and skipped rather than mirrored, and the extract records its own scope under `scope.ids`. Separately, `--link` was parsed with a falsy check, so an explicit `--link ""` fell back to the default instead of meaning no linked tables — asking for a single table produced three, with no error. An omitted flag now means the default and an empty one means none.

### geo-claim-grouping — 0.6.0
- 2026-08-21 · **HARD RULE 12: Podcasts-space claims (`b5a31f8182b042437ede0f84ee02f104`) are NEVER linked** (editor directive after the density + bracket rounds attached 195 Podcasts partners to debate claims — all unlinked the same day). Threefold enforcement: discovery refuses `--space`/`--pool`/`--seed` in the excluded set and drops excluded residents from semantic recall AND from the export via a live residency check (corpus pulls only carry the pull space — dual-resident copies are only catchable live); adjudication verdicts a slipped-through pair `EXCLUDED-SPACE` (no edge); the ops script's batched endpoint-residency guard emits `[SKIP excluded-space]`. Podcasts fix packages retired — the skill never generates ops bucketed to Podcasts. Dual-resident claims (Podcasts + editor space) are skipped and surfaced as editor calls.

### geo-claim-grouping — 0.5.0
- 2026-08-18 · **Step-1 Related bar recalibrated to TOPIC level** (team feedback: Related is the broadest grouping, popular claims need ≥10 links; the old "specific shared anchor" bar was step-2 thinking and left 74/143 debate claims at zero). New signal priority: curated block co-membership (no per-pair adjudication — the tab's own topic blocks ARE the judgment) → adjacent topic families for density top-up → cross-space same-topic attachments (debate-side direction counts toward page density on its own). Copy-collapse applies at every layer with a **negation guard** (J≥0.85 collapse only when no not/never/no/cannot token separates the pair — opposing positions and can/will modality variants never collapse). Delivered as the 2026-08-18 density round: 1,033 edges, 6 split proposals, projected 6→79 debate claims at ≥10 Related, zero at 0 (from 74).

### geo-claim-grouping — 0.4.1
- 2026-08-17 · **Copy-cluster collapse extended to ALL layers, step-1 Related included** (editor flagged 4 identical "backdoor" copy rows in a round-2 Related proposal; earlier reasoning had exempted the Related layer as a "census" — revised: the census lives in the decisions file + duplicates ledger, live edges never carry copies). One edge per distinct assertion everywhere; suppressed rows keep `dedupSuppressed`+`representative` in decisions; new near-dup clusters route to the duplicates ledger (backdoor ×4 turned out CRYPTO-resident — publishable dedup territory). Discovery bug fixed the same day (folded into 0.4.0's baseline work): the step-1 Related baseline only ANNOTATED already-grouped pairs — 47–54 of 80 export slots were wasted re-proposing them; `groupedPairs` now EXCLUDES pre-cap.

### geo-claim-grouping — 0.4.0
- 2026-08-13 · **QA-driven bundle refresh (deep audit of the skill + all published proposals; every reference file was still teaching the pre-taxonomy workflow).** `references/ops-script-template.md` REWRITTEN: two-step emission table, `brackets.json` schema (bracket/supported/mutual/rebutted/dedupSuppressed/representative), property-namespaced ids, BATCHED live checks (`id: { in }` chunks — replaces per-pair query loops), SAME-direction existing check, star-topology Duplicate linking, position-pinning + fix-package-overwrite warnings, live-vs-decisions reconcile verification. `references/queries-and-signals.md`: full 5-property table (argument-direction convention verified against ~2,700 pre-existing graph edges: FROM supported/opposed claim TO argument), per-property S3 pulls (Related pull = step-1 idempotency baseline), new quirks 9–11 (`relationsConnection id-in` batch works; ProposalVersion has no `executedAt`; `Position.generateBetween` jitters). `scripts/discover_candidates.ts`: S3 now also pulls the Root Related grouping `504e5776…` into `relatedPairs` so fresh step-1 campaigns skip already-grouped pairs. `evals/` replaced with 4 taxonomy-era evals (two-step order, strict re-adjudication, copy-collapse, star linking). HARD RULE 8 extended: star topology + distinct-assertion guardrails (different stats/modality/actors ≠ copies). File naming `claim-grouping-<slug>`.

### geo-claim-grouping — 0.3.1
- 2026-08-13 · **Copy-cluster collapse (new HARD RULE 8, editor rejection of the WA tier-A proposal):** N duplicate/near-duplicate partners of one target claim (cross-space copies, same-actor restatements) collapse to ONE edge to a deterministic representative — target's-home-space copy → distinct no-copy-sibling formulation → lowest id; the rest are `dedupSuppressed` in decisions (with a `representative` pointer) and routed to `duplicates-for-geo-clean.json`. Trigger case: 5 near-identical "Mamdani calls Netanyahu a war criminal" supporters staged on one claim. **HARD RULE 7 fixed: existing-edge pre-check is SAME-direction only** — the either-direction form silently suppressed intentional mirror directions on any rerun after the forward side went live (real incident: a delta rerun clobbered fix packages down to 1-op stubs; rebuilt by hand).

### geo-claim-grouping — 0.3.0
- 2026-08-13 · **RENAMED from geo-similar-claims** (editor decision — the skill now owns the whole grouping taxonomy, not just Similar) + **two-step process codified**: step 1 = Related grouping (pass 1, published/approved first — Related edges are terminal and stay whatever bracket the pair lands in); step 2 = bracket assignment (passes 2–3) with **mandatory RE-adjudication under the strict team definitions** — step-1 discovery verdicts (broad recall bar) never copy into brackets; most old tightly-coupled "similars" are RELATED-ONLY (the exact 08-12 review miscalibration). New step-2 bracket rubric in adjudication-rubric.md with the team's worked examples (near-identical wording → DUPLICATE; same core assertion reformulated → SIMILAR; material difference in scope/quantity/stage/facet → RELATED-ONLY; default-when-unsure RELATED-ONLY). **Argument relations now emitted by this skill in step 2** (previously delegated to geo-publish): `Supporting arguments` `1dc6a843…` directional FROM the supported claim TO its supporter; `Opposing arguments` `4e6ec5d1…` mirrored for mutual debate positions, directional for one-sided rebuttals. Context: pass-1 Related grouping (528 debate-tab edges) published+approved 08-12; old debate Similar edges unlinked 08-13 (98 deleteRelation via geo-clean) — clean slate for step-2 brackets.

### geo-similar-claims — 0.2.0
- 2026-08-12 · **Three-property taxonomy (Geo team review with Arturas):** many pairs marked Similar were actually Related, so the claim schema now carries three Root-space properties — `Related claims` `504e5776788844f6a77dba3ee811d8f0` (same broader issue/topic/event/context, distinct assertions), `Duplicate claims` `982866bf8ae94afe8cce8b805713e4af` (same claim created 2+ times, identical/near-identical wording), `Similar claims` `e81750db3f09440cab9dd01808a43ccb` (distinct formulations of the same core assertion) — reviewed via a mandatory **three-pass process**: pass 1 groups EVERY non-NOT-SIMILAR verdict (SIMILAR/DUPLICATE/SUPPORTS/OPPOSES) under Related claims; pass 2 marks Duplicates (linking ≠ merging — geo-clean still owns merges); pass 3 assigns remaining Similars (+ argument relations via geo-publish). Legacy Crypto-space `Related claims` `a8aa1b654a…` is superseded — never emitted. **Edge ids are now property-namespaced** (`typeId.slice(0,8)+from.slice(0,12)+to.slice(0,12)`, relation-entity id with from/to swapped) so the three passes can't collide on one pair; the old un-prefixed `from16+to16` scheme is retired for new edges (it already occupies the published Similar edges' ids). Idempotency pre-check now scoped to the SAME property. Rubric verdicts unchanged; SKILL verdict table now maps verdict → per-pass destinations, with a caveat that same-topic NOT-SIMILAR pairs may meet the broad Related bar as an editor call (never auto-promoted).

### geo-similar-claims — 0.1.0
- 2026-08-05 · **Editor-feedback round (pre-release, still 0.1.0):** (1) **bar broadened** — the 08-04 pilot's staged pairs were "almost exactly the same" (editor); SIMILAR now = assertion family PLUS tightly-coupled claims on the same specific event/process/metric/debate question (stage-of-process FLIPPED to SIMILAR; same-side debate positions in; tie-break with a specific shared anchor now leans SIMILAR); bar param `assertion-family-broad` (default) vs `assertion-family-strict`. (2) **Cross-space matching** — `--pool <spaces>` joins other spaces' corpora/signals; verdicts space-agnostic; ops bucket by the FROM claim's home space (HARD RULE 8), one held-for-review proposal per space. (3) **Page/tab scope** — `--scope-file {ids}` restricts exported pairs to those touching the scope; per-scope-claim semantic `search()` recall pass (claim-filtered, cross-space; verified live on v2 — flat list, EntityFilter arg; semantic-only pairs get a +0.5 admission bonus); tab→ids resolution recipe in SKILL.md. Exact-name clusters now computed over the pooled corpus with `crossSpace` flags.
- 2026-08-04 · Initial. Populates the `Similar claims` relation (`e81750db…`, Root-schema Claim property — greenfield: 1 edge graph-wide pre-skill, and it dangles) between Claim entities. Pipeline: bundled read-only `scripts/discover_candidates.ts` (space / seed / all-editor-spaces modes; text-first pre-cluster ported from the adjudication-validated 07-09 P7 pass + structural recall on the verified high-coverage signals — shared citation NAMES (Sources targets are per-claim entities named by article headline), Notable-claims story membership, Topics as bonus) → in-conversation adjudication per `references/adjudication-rubric.md` (SIMILAR / DUPLICATE→geo-clean / SUPPORTS·OPPOSES→argument flags / NOT-SIMILAR-when-unsure; two-bucket discipline, template-headline trap) → Discovery+Gates+Plan template + `go` → two-phase dated ops script (decisions.json read at runtime; deterministic per-direction edge ids `from.slice(0,16)+to.slice(0,16)` + pinned relation-entity ids (reversed concat — unpinned entityId randomizes per run); direction defaults to **both-ways** (mirrored edges, editor decision 2026-08-04); live idempotency/collision/liveness pre-checks; `createRelation` is the ONLY op kind) → explicit `publish` → held-for-review proposal. Gates: editor-access (fix-package route), duplicate/argument routing, already-linked, parameters (direction/bar/floor), volume caps (80 pairs / 200 ops). Verified against the post-migration v2 API (totalCount unusable — pull-and-count; `createdAt` = Unix-seconds strings; typed Value fields).
### geo-mirror — 0.11.0
- 2026-09-16 · **Part 2 works on any mirrored Notion table — `plan-notion-changes.mjs`.** Before this, `diff-notion-vs-geo.mjs` found only tables titled `Geo … — <space>` and mapped a fixed set of fields, so the "- new" pages and editor-made tables couldn't publish back. The rule is now: **a database with a `Geo ID` column is mirrored Geo content**, whatever it or its page is called. The planner searches a page (including toggles and column layouts; sub-pages with `--recursive`) or given `--db`s, and maps columns by name: title → Name, `Geo <X>` → X, `Proposed rename` → Name, `Proposed <X>` → X. Unprefixed columns count only in old-style tables, and only if the entities already carry that property or it is a system property. Property IDs come from Geo (the table's own usage first, then a unique name match with the SDK `SystemIds` as tiebreaker); ambiguous names are skipped. Two table styles: **proposal tables** (have `Proposed …` columns) publish only **Approved** proposals (`--setup` adds `Publish status` = Approved / Hold / Sent to Geo / Live on Geo), with the `Geo …` columns as the baseline and never published directly, since refreshes rewrite them; **direct tables** publish edited mirrored columns (the tested Part 2 behaviour), gated by approval only if the table has a `Publish status` column. Every change is checked against the **live value in the table's space**: rows outside the space, QA-flagged rows, fallback names, stale proposals and already-sent rows are skipped; values already live are marked `Live on Geo`; `--mark-sent` records what was published. `--preview-all` lists every filled value, and `sync-to-geo.mjs` now refuses to publish it. Relation, tag and hierarchy proposals are counted and left to geo-publish. `diff-notion-vs-geo.mjs` is now an alias for the planner. Shared helpers: `notion-geo-tables.mjs`. Also commits `mirror-claims-topics.mjs` and `mirror-sources.mjs`, which SKILL.md already documented but were never added to the repo. Testing caught one bug before release: an editor's `Notes` column was planned as a Geo change because Geo has a property named "Notes"; unprefixed columns now need the evidence above. Verified end to end on a scratch page without publishing (custom-named proposal table, old-style direct table in a sub-page, table without `Geo ID` ignored; planned/live/stale/fallback/QA cases; dry run; mark sent; re-plan), plus a read-only preview of AI - new (422 renames, 0 regressions).

### geo-mirror — 0.9.0
- 2026-09-10 · **`bulk-set-property.mjs` — stop filling Notion properties row-by-row** (perf investigation of a real editor task: proposing a hierarchy across the 482-row AI-topics database took **50 minutes**, and the measurements showed ~90% of it was writes, not thinking). Root cause: the Notion MCP's `update-page` takes **one page per call** (while `create-pages` batches 100), so ~450 rows = ~450 sequential agent round-trips at ~6.7s each. Measured for contrast: the whole *analysis* phase — assigning all 482 topics — took **~100s** with 4 sub-agents in parallel, i.e. ~3% of the runtime; parallelising the thinking saves ~2 min, scripting the writes saves ~45. New script writes one property across many rows as paced REST calls (~3 req/s, retry on 429/5xx, worker pool): relation/rich_text/select/url, row matching by any column, relation values resolved to page IDs in the same or another DB, **diffs first so only changed rows are written** (re-runs cost seconds), and reports rather than guesses on unmatched rows / unknown values / duplicate names. Verified live end-to-end on a scratch DB: dry-run classified 6 plan entries correctly (3 writes, ROOT skipped, missing row + unknown parent flagged), published 3/3 at 1.9 rows/s, immediate re-run reported `TO WRITE: 0`. SKILL gains a "Bulk-filling a Notion property" section incl. the synced-relation-pair rule (write one side only) and the integration-sharing 404 gotcha (scripts auth as the integration, not as you — MCP-visible ≠ script-visible). Full analysis: `documentation/notion-hierarchy-task-performance.md`.

### geo-mirror — 0.8.1
- 2026-09-07 · **Dedupe page Blocks + collection-items (doubled bodies fix) + space-aware diff.** Live test on the AI space surfaced that `uniqTargets()` was applied to relations but NOT to a story's page **Blocks** or their **Collection items** — so a sibling-dataset entity (present in both a space and its "… datasets" space) returned every block + claim **twice**, doubling every section heading and bullet in the Notion body (88 items where 44 exist). Fixed: `uniqTargets()` now wraps both the Blocks relations and each block's collection items; nested caps raised (Blocks 40→80, collection items 80→160) to absorb the doubling before dedupe. Part 2 `diff-notion-vs-geo.mjs` also hardened: reads each value's `spaceId`, prefers the editor's named space (falls back to a sibling copy rather than dropping a property), and targets the **named space** for write-back so edits never land on the sibling dataset copy. Verified live: AI-space mirror → 3 clean DBs, 0 duplicate body lines; World affairs story 12 items = 12 unique.

### geo-mirror — 0.8.0
- 2026-09-03 · **Type-generic — mirror ANY entity type, not just News stories** (editor: "this skill should mirror all spaces, why isn't it working?" — a podcast mirror hit the News-only guard). Rebuilt the engine to be type-agnostic: `extract-space.mjs` takes `--type <id>` (default News story) + `--related <entityId>` (e.g. episodes of a podcast, stories about a topic) and captures each entity's **own** scalar values, all relations grouped by type, and page Blocks. `mirror-to-notion.mjs` creates a DB for the **primary type + core content relations only** (default: Notable claims → Claim, Sources → Article/Project) — a lean **~3-DB** result, not one-per-related-type (an over-generation caught in testing: a News mirror spawned 10 DBs incl. Country/City/Place). Every other relation (Topics, Related people, Hosts, Guests, Podcast…) is mirrored in the primary row's **page body**, not a separate table; `--link "…"` opts more relations into their own DBs. Columns from each type's values; relations as linked columns; primary body from Blocks + relations. Fixed a self-referencing-type bug (same-type related rows were dropped). Part 2 `diff-notion-vs-geo.mjs` generalized to diff **every** `Geo … — {space}` DB's editable text/url columns (no hardcoded field list). Auto-detects the type's date property (Publish datetime / Air date). Verified live: News (World affairs) AND **The Daily podcast episodes** (Podcasts) both extract + dry-run correctly — Episode DB (Air date/Duration/Audio URL) + Podcast/Person(Hosts,Guests)/Claim/Topic/Project DBs, relations linked. Replaces the fixed Stories/Claims/Sources three-DB model.

### geo-mirror — 0.7.0
- 2026-09-03 · **Part 2 — sync Notion edits back to Geo.** After an editor edits mirrored content in Notion, push the changes back. Two new universal scripts: `diff-notion-vs-geo.mjs` (read-only; reads the Notion rows, compares editable fields against the CURRENT Geo entity by Geo ID, emits a field-level change plan `old → new` — never blanks a Geo field from an empty cell) and `sync-to-geo.mjs` (turns the plan into `Graph.updateEntity` ops and publishes via the repo's `publishOps` — personal-vs-DAO routing + circuit-breaker; DRY_RUN default, `--publish` to write). Two-phase gate like geo-publish: diff → review → dry-run → publish; DAO spaces become a proposal+vote. **v1 syncs editable fields** — Story Name/Summary/Description, Claim Name, Source Name/Web URL; structural edits (add/remove claims/relations, Publisher, page-body, new entities) are a later increment. Verified: op-building + dry-run on a synthetic plan (Graph.updateEntity → 1 op, nothing published). SKILL gains a Part 2 section + GEO write gate.

### geo-mirror — 0.6.0
- 2026-09-03 · **Scope safety gate — never mirror a whole space** (editor feedback: an editor might pass only a space; Geo spaces are huge and grow daily, so an unbounded mirror would flood Notion). The extractor now **refuses to run (exit 2) with no narrowing dimension** — requires a date range (`--since`/`--until`), a `--topic <id>`, or a `--limit N`, or an explicit `--all` override (confirm loudly). Added the `--topic` filter (stories carrying a given Topic). SKILL scope gate is now HARD: if the editor gives only a space, STOP and ask which tab/feed, topic, or date range. Documented that the **News** tab = this skill's News-story sweep (verified the tab filter is `type = News story`); other tabs (Events, Governance, People) are different entity types → a follow-up, not silently mirrored. Verified: no scope → exit 2 with guidance; `--topic` narrowed 25→1 story live.

### geo-mirror — 0.5.0
- 2026-09-03 · **Stories table first (the editor kept opening Sources and thinking it was empty).** The three DBs were created Sources→Claims→Stories, so **Geo Sources sat on top** of the page — the editor opened it, saw sparse article rows, and couldn't find a News story (e.g. "Uber announces layoffs of 3,300…" is a *story*, but Sources holds the differently-named source articles). Now DBs are created in **page order Stories → Claims → Sources** (Stories = the primary, rich, editable table on top), with the cross-DB relation columns added in a second PATCH pass (a relation needs its target DB to exist first). Existing pages from an older run keep their old order — delete the 3 DBs and re-run once to reorder. The story-body mirror (grouped sections) is unchanged.

### geo-mirror — 0.4.0
- 2026-09-02 · **Faithful page-Blocks mirror — grouped sections, not a flat list** (test feedback: the Geo News page shows claims grouped under section headings — "Wong collusion guilty plea", "Foreign sanctions conspiracy charge" — but 0.3.0 dumped one flat "Notable claims" list; "I still don't see what I see on geo"). Root cause: a News-story page is composed of **Data blocks** (position-ordered), each with a heading (Name), intro (Description) and **Collection-item** relations → the specific claims it groups; 0.3.0 walked the flat Notable-claims relation and ignored the Blocks. Now the extractor resolves each story's page Blocks (`beaba5cb…`, POSITION_ASC) with their Collection items (`a99f9ce1…`), and the writer renders each block as `heading + intro + its claims` — reproducing Geo's exact grouped structure; then Related stories / Sources / Related entities / Topics. Flat Notable-claims fallback for stories with no blocks. Verified live on the Joshua Wong story: 5 sections, 44 blocks, matching the Geo page 1:1.

### geo-mirror — 0.3.0
- 2026-09-02 · **Story pages mirror the full Geo article body** (test feedback: opening a mirrored row showed only database property fields, not the rich article the Geo entity page renders — "why can't I see this data … like literally mirroring?"). The mirror wrote columns but left each Notion page **body empty**. Now each **Story page body** is built as content blocks mirroring the Geo page: description paragraph, **Summary** section, **Notable claims** as a bulleted list, **Sources** as linked bullets, **Topics**, and **Related** people/entities/stories — replaced idempotently on re-run (`replaceChildren`). Extractor extended to pull Related people/entities/stories. Columns still drive the table/list view; the body is the readable mirror. Verified on a real story (Joshua Wong): 34 blocks (Summary + 20+ claim bullets + sources + topics) matching the Geo page.

### geo-mirror — 0.2.1
- 2026-09-02 · **Databases render inline** (test feedback: the three mirrored DBs showed as collapsed sub-page links on the parent page, not visible tables — the user expected an embedded table). Root cause: databases created via the Notion API default to full-page rendering (a link block), not inline. Fix: create with **`is_inline: true`** so each renders as a **full table embedded in the parent page**; the find branch also PATCHes any pre-existing DB to `is_inline: true`, so a re-run fixes an already-mirrored page with no delete. (Views/grouping/view-tabs still can't be API-created — unchanged.)

### geo-mirror — 0.2.0
- 2026-09-02 · **Richer Notion presentation** (test feedback: first mirror produced bare database links, not the news-feed / rich-table look expected). Stories now carry a **Cover** image — the Geo Cover (`ipfs://` Image) is resolved to an HTTPS gateway URL (`gateway.pinata.cloud`, override via `IPFS_GATEWAY`) and set as the Notion page **cover + icon** + a Cover property, so a **Gallery view renders like Geo's News feed**. Added a **Review status** select (`To review`/`Reviewed`/`Edited in Notion`) on Stories + Claims — the editor workflow column and the Part-2 change hook. SKILL now states plainly what the Notion API **cannot** do (create views/grouping/view-tabs — a one-time manual Gallery-view step per DB) so the layout expectation is set upfront. Verified live: US Politics covers 10/10 resolved (200 image), writer dry-run clean.

### geo-mirror — 0.1.0
- 2026-09-02 · **Initial — Part 1 (Geo → Notion mirror).** New skill: mirror a Geo space's News stories + their Notable claims, Sources, and Topics into three **linked Notion databases** (Stories ↔ Claims ↔ Sources), each row keyed by its **Geo ID** so re-runs update in place and Part 2 can diff. Two universal reusable scripts: `extract-space.mjs` (Geo → normalized JSON, read-only, no key; scoped `spaceId`+`typeId` sweep, bounded nested relations per geo-query "Memory blow-up"; date filter on the **Publish datetime** property, not epoch `createdAt`) and `mirror-to-notion.mjs` (JSON → linked Notion DBs via the Notion API with a `NOTION_TOKEN`; find-or-create DBs, upsert every row by Geo ID, `--dry-run`). NOTION gates (token present without printing, connection confirmed via `/users/me`, integration shared into parent page, scope+range+parent confirmed) + read-only on Geo in Part 1. Verified live: US Politics since 2026-08-19 → 94 stories / 1336 claims / 386 sources extracted; Notion writer dry-run clean. Part 2 (Notion → Geo diff + sync-back through geo-publish gates) is next.

### geo-publish — 0.11.0
- 2026-09-11 · **Mandatory table-based dry-run report** (editor report: "I'm not always reviewing the reports carefully, partly because it isn't presented very clearly — just by looking at them I don't really understand if any mistakes are being published"). A gate nobody can scan isn't a gate. Root cause in the skill: the dry-run step only said *"surface op count + first-op sample"*, so the format was left to the model and came out as unscannable prose. Replaced with a **mandatory 6-section report**, always in order, sections kept even when empty: **1** what gets written (name/type/op), **2** every value with its declared-vs-written data type side by side (makes the silent datetime-as-date class visible), **3** relations incl. target existence + toSpace, **4** all five gates as a pass/fail table, **5** op totals (catches "I meant 5 rows, not 500"), **6** **⚠ Needs your eyes** — a plain-English list of anything the agent guessed, inferred, truncated, skipped or could not verify. Section 6 is the core addition: every silent-failure class we've hit (slug-derived headline, fabricated URL, dropped field, waived duplicate, cross-space write) is invisible in an op count and obvious in one sentence; explicitly must not be left empty to look clean. Bulk publishes show first 3 rows + totals **plus every unusual row**, never a sample that hides outliers. HARD RULE 4 updated to require the format.

### geo-publish — 0.10.1
- 2026-09-11 · **Stop the false "you have no private key" alarm** (editor report: the agent sometimes reaches the `go`/`publish` gate and announces the wallet key isn't set up on a machine where it *is*; the editor has to insist before it proceeds). Three causes, all fixed: (1) the check `cat .env … | grep` is **routinely refused by the permission classifier as secret-access**, and a refusal is indistinguishable from "no key" — evidenced in a live transcript ("the env check was blocked (it involved cat on .env)… the .env grep is blocked by the classifier too") — so the check now tests for the **file's existence only** (`ls -a .env .env.geo-publish | grep -q .`, the `grep -q .` guarding the same false-negative shape as the old `grep -qs` bug, since `ls` exits non-zero when *any* argument is missing); (2) a relative `.env` check run from the wrong cwd reported "missing" — the check now prints `cwd` when it finds nothing, so the real problem is visible; (3) nothing forbade re-asking, so the question resurfaced at the gate — new HARD RULE 4 clause: **never re-open environment/setup questions at the `go`/`publish` gate**, and the key is never raised more than once per session (a user saying it's set is authoritative and final). The check is now explicitly a **soft check, not a gate** — blocked/unreadable ≠ missing; bias toward proceeding, because the publish itself is the authoritative test. Also fixed the runtime error that named the wrong variable: `src/functions.ts` said `"PK_SW not set in .env"` (the *legacy* name) and now reports `GEO_PRIVATE_KEY not found…` plus the cwd it looked in.

### geo-publish — 0.10.0
- 2026-08-21 · **Ontology consult — correct-type check via `ontology-advisor` (new Gate 0)** (shadowing memo A2: an editor pastes an X link + "publish to AI space" and gets a bare `Post` — no text/topics/author/description — instead of a `Tweet`; ~50 published this way). geo-publish now consults **ontology-advisor** during Discovery to pick the *correct* type + expected properties **before** proposing, rather than depending on a perfect prompt. New **Gate 0 (ontology / correct-type)** fires when the type is wrong for the content (x.com → Tweet not Post; reddit → Post; article → Article; bio → Person) or its expected properties are missing (Tweet: text/Author/Topics), and the proposed type shows in the dry-run. Read-the-reference by default (`ontology-advisor/references/ONTOLOGY.md`), invoke the skill only when modelling is ambiguous. HARD RULE 11 + Gate 0 dialog + Discovery "Type (per ontology-advisor)" line + Discovery how-to step. Complements inject-mode (0.9.0), which already types pasted URLs via the injector — Gate 0 confirms it; the consult is the safety net for hand-built entities. `ontology-advisor` is unchanged and still fully standalone (one-way consult).

### geo-publish — 0.9.0
- 2026-08-17 · **Inject mode — publish from a pasted URL** (field report: an editor couldn't publish an X post; the news-worker "injector" was never wired to the skill). New "Inject mode" section: when the editor pastes a URL (tweet / news / reddit / wikipedia), call the injector, decode its base64 GRC-20 Edit to ops, and publish through the SAME gated path (Gate 1 dup-check on the extracted name → `go`/`publish` → `publishOps`, personal-vs-DAO routing + circuit-breaker intact). Added reusable `lib/inject.ts` (`detectInjectType` host-detect + `injectAndDecode` = inject→poll→decode), pinned `@geoprotocol/grc-20` in package.json, and a worked example `scripts/inject-publish-example.ts`. Config via `.env` `INJECT_BASE_URL`/`INJECT_API_KEY` (staging `news-worker-production.up.railway.app`, cron-disabled; NOT the live `news-worker.up.railway.app`); prod auth → Privy later. Verified end-to-end: tweet → 14 ops → published + indexed in a personal space.

### geo-publish — 0.8.2
- 2026-08-11 · **Bun `fetch` gotcha (publish stalled on "socket closed / retrying").** In some sandboxed environments Bun's `fetch` can't reach `api-testnet.geobrowser.io` while `curl` and Node's `fetch` reach it fine — so a dry-run passes but `bun run` hangs at the publish/broadcast step with nothing written. Runtime prereq now says **prefer Node for the publish step**, and if `bun run` stalls on network, re-run with `node --env-file=.env scripts/<file>.ts` (Bun quirk, not an outage). Surfaced during a live Cowork publish (Ingrida Šimonytė).

### geo-publish — 0.8.1
- 2026-08-10 · **Publish-time network hosts documented (new prerequisite 4).** Dry-run succeeds but `publish` fails at the IPFS step when the sandbox egress allowlist only has the reads host (or the old pre-migration `testnet-api`). Listed the three hosts publishing actually needs — `api-testnet.geobrowser.io` (IPFS upload), `rpc-geo-testnet-irdc0cgb0w.t.conduit.xyz` (tx RPC), `rpc.zerodev.app` (gas sponsorship) — with the symptom + that it's an org-admin/environment setting, and the fall-back of handing the script to the user to run locally.
- 2026-08-10 · **Key-check fix (editors were falsely blocked from publishing).** The prerequisite check only looked for `.env.geo-publish` (GEO_PRIVATE_KEY) or `.env` (legacy `PK_SW`) — it never checked `.env` for `GEO_PRIVATE_KEY`, which is exactly what the setup guide creates. So editors who set up correctly got "no wallet key configured" and had to explicitly say "just use .env". Now the check accepts all valid setups (`grep -qsE '^(GEO_PRIVATE_KEY|PK_SW)=' .env .env.geo-publish`) and `.env` + `GEO_PRIVATE_KEY` is documented as the canonical location. Added an explicit "do not block when `.env` already has `GEO_PRIVATE_KEY`" note.

### geo-publish — 0.8.0
- 2026-08-01 · **v20 migration validated live (post-cutover).** SDK pinned `^0.20.2` (stable); config auto-adopts the final `api-testnet` origin + updated space registry with no code change. **Fix: lowercase the wallet address in the personal-space lookup** — the new infra stores space addresses lowercase with a case-sensitive `is` filter, so the checksummed address returned by the wallet client matched nothing and every publish aborted. Full path smoke-tested end-to-end on a personal space (op build → EIP-7702 gas-sponsored wallet → `publishEdit` → on-chain tx on chain 55516 → indexed + read back → deleted). Pre-cutover writes confirmed not carried over.
- 2026-07-27 · **v20 migration (breaking — SDK 0.13/0.19 → 0.20).** All script templates ported to the v0.20 client API: `createGeoClient({ network: GeoTestnetConfig })` (string network IDs are gone), `getSmartAccountWalletClient` → `createGeoWalletClient({ signer: privateKeyToAccount(pk), network })`, `personalSpace.publishEdit` → `geo.personalSpaces.publishEdit` (no `network` param), `daoSpace.proposeEdit` → `geo.daoSpaces.proposeEdit`, flattened `voteProposal`/`executeProposal`. Voting-settings renames documented (`slowPathPercentageThreshold` → `partialPercentageSupportThreshold`, `fastPathFlatThreshold` → `flatSupportThreshold` + 3 new required fields). **Endpoints, chain id (55516) and contract addresses all come from `GeoTestnetConfig` — nothing hardcoded.** Prereqs now require SDK v0.20+ post-migration (older SDKs publish to retired contracts — edits silently lost after the grace window). `Graph.*` op builders and all four gates unchanged.

### geo-publish — 0.7.0
- 2026-07-17 · **Type-required safeguard (HARD RULE 10 + Gate 4)** — no entity publishes without at least one type (any type; none is not acceptable). Covers EVERY created entity incl. inline relation targets and per-row dataset creates; block entities pass by construction. Blocked ≠ dead end: the gate proposes types instead of just refusing. Updates to already-typeless entities WARN (repair stays possible); datasets without a Types column stop for editor input. New `createTypedEntity` wrapper is mandatory in generated scripts (runtime belt behind the doc-level suspenders) + dry-run now prints `Creating: <name> [<type>]` per entity. From a 16 Jul field report (untyped entities from publishes + multi-space creation); platform-side enforcement raised separately with the core team. Evals 3–4 added.
- 2026-07 · **Page-block recipes** (Monday QA, all op shapes verified live on canvas `b91409f7…`): new "Publishing page blocks" section — well-known block IDs; text blocks (markdown, inline entity mentions via `graph://<id>`, code/formula as fenced markdown — no separate block types); media (image via `createImage` + **bad-fetch guard**: SDK uploads HTML error pages silently, tell = no Width/Height; cover/avatar rels; mp4-only video); data blocks (collection/query/ranking — **View & Columns live on the Blocks-relation entity id**, same edge-vs-entity trap as Gate 3; collection row order = item positions, 9/page); tabs (Page entity via Tabs prop); block idempotency (fresh id per call duplicates on re-publish → deterministic ids); block-order read-back caveat (`POSITION_ASC` collates case-insensitively — sort client-side). Review: all 13 IDs + SDK exports re-verified live.
- 2026-07 · Spreadsheet-source note in "Bulk / dataset publishing": the publish path reads CSV/JSON, not `.xlsx`/`.xls`/Sheets/Notion — convert first. Editors publish from the repo, so lead with the in-repo converter `src/xlsx-to-csv.cjs` (SheetJS; `xlsx` dep); manual export (Excel/Sheets/Notion→CSV) as the fallback. Validated end-to-end: `.xlsx` → CSV → 3 Person entities published to a personal space.

- 2026-07 · Data-type check generalized beyond datetime: full `dataTypeName` → SDK value-type mapping table (all 11 live dataTypes enumerated from the API, incl. Checkbox→boolean and Relation→never-a-value); Gate 2 now checks EVERY planned value against it. Zero extra queries — dataTypeName already comes back in discovery, so no slowdown for bulk/ranking runs.
- 2026-07 · **Relation entity-id + dataType fixes** (from a live diagnosis of the ~1000-row mis-publish): HARD RULE 7 + "Relations — entity id vs relation id" section (knowledge goes on the relation `entityId`, never the relation `id`; use `createRelation`'s `entity*` params or resolve `entityId` to update — new Gate 3). HARD RULE 8 + Gate 2 extended: match the property's declared `dataTypeName` (datetime-vs-date silent-render trap). HARD RULE 9: test one row (visually) before bulk. Fixed the discovery query too (validated: `types{ id name }`, `nodes{}` wrappers, `dataTypeName`, relation `entityId`).
- 2026-06 · **Runtime data loading rule** (fixes the bulk-publish outage): scripts must READ the dataset file (CSV/JSON) at runtime, never transcribe rows into the script as constants — baking rows in times out on large sets and risks the model fabricating values (esp. URLs). New "Bulk / dataset publishing" section + HARD RULE 6.
- 2026-06 · Consolidated to one portable skill (GEO_PRIVATE_KEY, PK_SW fallback) + strong safeguards (dup check, schema gate, two-phase dry-run→confirm). Retired geo-publish-codex.
- earlier · 0.1–0.2 pre-consolidation publish skills.

### geo-clean — 0.5.0
- 2026-08-21 · **Ranking awareness + system-entity exclusion** (follow-up to the 2026-08-18 full-graph discovery review). (1) **Ranked entities surfaced everywhere**: documented the live ranking model — **Ranking** entities (type `5c74731d…`, usually in rankers' personal spaces) cast **Ranking Vote** relations (`19a4cfff…`, the id previously described as "Rank Votes") at ranked entities; "Submitted to Ranking Block" (`09c219c1…`) attaches them to pages; Score aggregates votes (verified: Animal Farm, 30 inbound votes). Every discovery output now carries a `Ranked` (vote-count) column + `System entities excluded` / `Ranked candidates flagged` counters; new **Gate — Ranked entity** on delete-orphan: ≥1 inbound Ranking Vote ⇒ per-entity `delete ranked {id}` confirmation, never part of a bulk delete (votes are community data; HARD RULE 8 wording updated to the live "Ranking Vote" name). (2) **New HARD RULE 12 — system entities are invisible to cleanup**: governance `Proposal <uuid>` entities (protocol-minted, incl. post-migration "System entity for proposal…" stamps — the old "husks: default delete" guidance is retired), `Space <uuid>` payout/allocation bookkeeping entities, and Ranking entities are never listed as candidates, never counted as junk, never targeted by ops (`[SKIP] (system entity)`); find-untyped output re-bucketed (system/relation-entity/stub/real) with aggregate-only exclusion counts. reference.md: gotcha 11 rewritten, new "Ranking model & system-entity detection" section with verified ids + detection recipes + the cheap ranked-check query. Evals 12–13 added.

### geo-clean — 0.4.0
- 2026-08-18 · **Anchored-entity protection + dry-run expiry + circuit-breaker wiring** (post-mortem of an Aug-2026 personal-space wipe: a bulk delete removed 11,410 entities *including the profile photo and space description* because nothing marked identity entities as protected, the `publish` came 10.4h after the dry-run, and the deletion ran through a hand-written raw-SDK loop that bypassed every safeguard). Three new HARD RULES: (9) **anchored entities excluded by default** — new `getAnchoredEntityIds(spaceId)` helper (`src/functions.ts`, verified live) resolves a space's `page`/home entity + Avatar + Cover image entities from `ANCHORED_IMAGE_RELATION_TYPE_IDS` (`src/constants.ts`); space wipes/bulk deletes skip them (`[SKIP] (anchored)`), require `delete anchored too` override, and print `Anchored entities excluded: N`; (10) **dry-run authorizations expire** — >30 min between dry-run and `publish` (or an interrupted session) forces a fresh dry-run; (11) **never hand-write a raw-SDK delete loop** — all destructive ops go through `src/` helpers + `publishOps`, whose >50-destructive-op circuit-breaker requires `CONFIRM_DESTRUCTIVE=1` set ONLY on the confirmed publish run (never the dry-run, never to route around a block). "Delete space data" op gains a Gate — Anchored-entity + updated confirmation/plan copy; reference.md gains anchored-exclusion + circuit-breaker sections.

### geo-clean — 0.3.1
- 2026-07-28 · **2026-07 infra migration** (breaking SDK v0.20.0 + new endpoints): new HARD prerequisite 4 — repo must be migrated (SDK ≥ 0.20.0, endpoints derived from `GeoTestnetConfig`; mechanical grep check), STOP if not: through the grace window the old endpoints keep responding while edits published there are silently not carried over. Allowlist prerequisite lists the config-derived hosts (currently `testnet-api-v2.geobrowser.io` GraphQL — announced `api-testnet` alias serves the same data; new Conduit RPC, chain 55516; `rpc.zerodev.app` sponsorship). reference.md: new "Infrastructure & SDK" section (config-derived endpoints — announced vanity RPC not live as of 07-28; `createGeoClient`/`GeoTestnetConfig` — string network ids gone; wallet = EIP-7702 `createGeoWalletClient` via new `getWalletClient`/`getWalletAddress` helpers, `getSmartAccountWalletClient` removed; flattened `daoSpaces.voteProposal`/`executeProposal`; `Graph.*` deprecated-but-working; voting-settings renames + 3 new required fields) + script-generation rule: never hardcode endpoints/chain-ids/addresses; functions.ts helper list extended. Eval 11 (unmigrated repo → STOP, no workaround). Paired same-day repo code migration: `src/functions.ts` + `src/entity_ops.ts` + `package.json` (`^0.20.0-beta.8`), verified read-only on the new infra (gql + SDK client + wallet → personal-space lookup all pass).
- 2026-07 · **Adopted the geo-merge-topics cleanup system** (ported `src/select_canonical.ts`, `validate_migration.ts`, evolved `entity_ops.ts`/`constants.ts`/`functions.ts` — local `allowCanonicalDelete` guard retained) + the Notion topic-reference rules, generalized to all types. Merge op rebuilt: **deterministic canonical cascade** (canonical-space representative topic → Root (Geo) resident → not-catch-all-only → Featured → Scored → Curated → true backlink totalCount → data → age → id) with hard exclusions (personal/dataset copies never canonical — Root exception) and editor override; **Both-Scored escalation** (≥2 scored members → never merged, escalated to review); **untouchable-space vacate semantics** (personal/dataset copies survive; representative topics never vacated from their own space); **references-only cross-space merges** (merge never grants the canonical new residencies) with an **END STATE plan block** + **Root-never-loses-a-topic invariant** (no override); **HARD RULE 8: voting data untouchable** (Score / Rank Votes / vote values excluded from every op + scrub pass — merges previously fabricated/destroyed votes); post-publish **migration validator** (snapshot → publish → 3-rule check) + per-space governance proposal URLs; fix packages for no-access spaces; escalation reports. New **Move/copy operation** (references preserved; representative-topic guard). Fixed skill↔code drift: `disableAutoSelect` now makes the helper respect the designated Main (it silently re-picked before); reference.md's fictional `summaryOut`/`keepAsGhost`/`LEGACY_*` contract replaced with the real API; big-merge.md wired to the validator. Evals 4–10 added.
- 2026-06 · Runtime data loading rule (HARD RULE 7): cleanup scripts read the `<date>-*.json`/CSV list at runtime, never embed IDs/rows as constants — avoids timeouts and ID corruption on large lists.
- 2026-06 · Initial. Find/merge duplicates, delete orphans, fix data types/stale relations; bundled reference.md + big-merge.md; Discovery→Gates→Plan→dry-run→publish safeguards.

### geo-orchestrate — 0.2.0
- 2026-06 · Runtime data loading rule in script-generation: generated scripts read the dataset file at runtime, never transcribe rows as constants; fixed the "Add Web URL from CSV" job accordingly.
- 2026-06 · Initial. Intent → plan → generate script → dry-run → confirm → publish; routes to query + publish.

### geo-discovery — 0.1.1
- 2026-08-12 · **Stale endpoint fix** (field report: skill's query hit the decommissioned `testnet-api.geobrowser.io` — the pre-migration word-swapped host — and was rejected by the network allowlist). `ENDPOINT` in `scripts/publish_gaps.mjs` + `scripts/build_dashboard.mjs` re-pointed to `api-testnet.geobrowser.io/graphql`, matching `lib/gql.mjs`. Unblocks the read/query path. **Known debt:** the publish path is still on the v0.19 SDK API (`getSmartAccountWalletClient`, `daoSpace.*`/`personalSpace.*`, `network:"TESTNET"`) that v20 removed — a full migration to `createGeoClient` / `geo.daoSpaces.*` (as geo-publish/geo-clean already got) is still pending, and there is no SDK-version guard here yet.

### geo-discovery — 0.1.0
- 2026-06 · Initial. 6-stage gap-discovery pass over a space's daily stream → ranked Gap findings; read-only until human-reviewed publish stage. (Version normalized from `latest`.)
